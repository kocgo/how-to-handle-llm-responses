UI (Vite + React + TS)

Commands

- npm run dev - start vite dev server
- npm run build - build
- npm run preview - preview build
- npm run test - run vitest (the example Playwright test expects the dev server running)

Playwright

After installing dependencies, run `npx playwright install` to download browser binaries.
