Server (Node + TypeScript)

Commands

- npm run dev - run with ts-node-dev
- npm run build - compile to `dist`
- npm run start - run compiled output

This server intentionally uses no external web framework for minimalism.
